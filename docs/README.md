# GitHub Pages site

`index.html` is generated by `python3 scripts/update_promotions.py`. Commit it and push to publish.

Public URL (after Pages is enabled):

`https://<your-github-username>.github.io/<repository-name>/`
